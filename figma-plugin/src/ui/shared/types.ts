export type UserData = {
  name: string;
  photoUrl: string;
};

export type CapturedImage = {
  nodeId: string;
  nodeName: string;
  screenshotUrl: string;
  thumbnailUrl: string;
  dominantColor: string;
};

export type TagItem = {
  slug: string;
  name: string;
};
